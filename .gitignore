__pycache__/
*.pyc

# Locally-built Anthropic bundle — never commit (built on-device by install-latest.sh)
cli.js
bundle*.mjs
bundle*.cjs
bundle.v8cache
*.v8cache
claude-*.cli.js
node_modules/
*.bin

# bench/ harness: keep source (scripts, *.go, *.cjs, *.py) + README + results tables;
# drop all heavy/derived/staged artifacts (regenerated by prepare.sh / run.sh).
bench/input/
bench/build/
bench/node-baseline/
bench/**/*.cache
bench/goja-host/goja-host
bench/goja-host/dist/
bench/**/.compile-cache/
